import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        secondary: {
            main: "#52576D",
            light: "#373B4B",
        },
    },
});

export const colors = {
    gold: "#C6B5A5",
};
